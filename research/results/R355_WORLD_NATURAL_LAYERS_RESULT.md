# R355 Result — World-Natural Neural Layer Algebra

Status: **compositional PNS layer-law gate passed; not DoD and not a novelty claim.**

## Rule

A lifted pre-materialization block is legal when the materialization square commutes for every legal world:

`M_W(L^(K)) = L(M_W(K))`.

World-natural blocks can be composed while preserving the same law. A decoder-visible early fusion of historical mutable payload is outside this algebra unless a stronger exact symbolic/certified lift exists.

## Result

- random prospective programs: **1,200**
- layers/program: **8**
- future worlds/program: **128**
- total tested lifted layers: **9,600**
- maximum commuting-square absolute error: **1.776e-15**
- downstream observable mismatches: **0**
- retained-PNS digest changes from world evaluation: **0**

The matched retrospective early-value-fusion control produced:

- downstream mismatches: **61,902**
- maximum downstream logit delta: **7.0771**.

Layer mix included world-independent nonlinear continuation, reference linear transforms, residual reference mixing, stable reference permutation and query-state-controlled reference scaling.

Report SHA256: `7f60f31f7be18f02dda3f86971d220654b5b4500c5bdc13818600fce3c47a1a3`.
Artifact ZIP SHA256: `70c0bdca7d1b8202a320ee0b3e829cf9b780c663ec4f5756bd4f5398cc309076`.

## Architectural decision

Promote **world-naturality** as a compiler/interface law for PNS:

> The B/PNS region is not defined merely as “all layers before layer N”. It is the maximal neural region for which the current-world materialization operation commutes with the lifted layer semantics.

This gives a principled way to move the mutable-world boundary through a network and connects R351's cache normal form to actual layer composition. R352 can serve as an optional proof mechanism to lift additional nonlinear blocks when their world-sensitive control is certifiably stable.

Naturality, lifted operators, staged computation and partial evaluation are established ideas. R355 provides a CKCA/PNS compiler formalism and falsification test, not a standalone novelty claim.
