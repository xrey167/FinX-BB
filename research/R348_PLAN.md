# R348 plan

Fast frozen-Qwen PNS bridge: final-token Qwen representation -> world-independent continuation code; canonical refs stay live; current values are dereferenced only at J materialization. Small prompt set keeps the real-pretrained experiment within hosted CPU CI.
