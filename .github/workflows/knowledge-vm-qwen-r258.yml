name: Knowledge VM Qwen R258 parametric bridge

on:
  push:
    branches: [research/knowledge-vm-real-gate-20260913]
    paths:
      - 'research/qwen_knowledge_vm_r258.py'
      - '.github/workflows/knowledge-vm-qwen-r258.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qwen-r258:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      HF_HUB_DISABLE_XET: '1'
      TOKENIZERS_PARALLELISM: 'false'
      SO_R258_REPORT: ci-qwen-r258/report.json
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install pinned CPU runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install transformers==4.48.3 tokenizers==0.21.0 safetensors==0.5.2 huggingface_hub==0.28.1
      - name: Run real Qwen parametric capability pointer gate
        run: |
          mkdir -p ci-qwen-r258
          python research/qwen_knowledge_vm_r258.py | tee ci-qwen-r258/stdout.log
      - name: Archive R258 evidence
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: knowledge-vm-qwen-r258-parametric-bridge
          path: ci-qwen-r258/
          if-no-files-found: warn
