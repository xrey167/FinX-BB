name: E000095 semantic-address exact-payload baseline
on:
  push:
    branches: [research/e000095-semantic-address-exact-payload]
    paths:
      - 'so/experiments/e000095_semantic_address_exact_payload.py'
      - 'docs/novelty/e000095-preregister.md'
      - '.github/workflows/e000095-semantic-address-exact-payload.yml'
permissions:
  contents: read
jobs:
  semantic-address:
    strategy:
      fail-fast: false
      matrix:
        seed: [0, 1, 2]
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      SO_BOS: '1'
      OMP_NUM_THREADS: '2'
      OPENBLAS_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      TOKENIZERS_PARALLELISM: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: so/requirements.txt
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -r so/requirements.txt
      - name: Compile experiment
        run: python -m py_compile so/experiments/e000095_semantic_address_exact_payload.py
      - name: Run preregistered E95 seed
        run: |
          set -o pipefail
          mkdir -p ci-e95
          python -m so.experiments.e000095_semantic_address_exact_payload \
            --seeds ${{ matrix.seed }} --steps 3000 --groups 100 --threads 2 --results-dir ci-e95 \
            2>&1 | tee ci-e95/stdout-s${{ matrix.seed }}.txt
      - name: Hash evidence
        if: always()
        run: |
          sha256sum so/experiments/e000095_semantic_address_exact_payload.py > ci-e95/source-sha256-s${{ matrix.seed }}.txt
          find ci-e95 -maxdepth 1 -name '*.json' -print0 | sort -z | xargs -0 -r sha256sum > ci-e95/result-sha256-s${{ matrix.seed }}.txt
          python -m pip freeze > ci-e95/environment-s${{ matrix.seed }}.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e000095-semantic-address-exact-payload-s${{ matrix.seed }}
          path: ci-e95/
          if-no-files-found: error
