# E-000042 — a certified lower bound for deletion in a representation, from the J-lens support

Frozen gpt2, layer 7, no training. For each fact, EVERY subset of an 8-direction pool
is ablated and the answers recorded, so the true optimum is found by enumeration and the
bound is compared against the answer rather than against another estimate.

## VOID

No claim is made: fewer than three facts the pool can silence; there is nothing to bound

## Pre-registered criteria

| criterion (worst seed) | required | observed | result |
|---|---|---|---|
| control_bound_can_exceed_one | >= 1.0 | 1.0000 | PASS |
